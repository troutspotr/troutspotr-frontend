const StateData = require('static/data/v3/wi/wi.data.json')
const RegionData = require('static/data/v3/wi/highland-headwaters.topo.json')

describe('api/GeoApi.transform', () => {
  // let stateData = null
  // let regionData = null
  beforeEach(() => {
    // stateData = updateStateObject(_.cloneDeep(StateData))
    // regionData = updateStateObject(_.cloneDeep(RegionData))
  })

  describe('decompress', () => {
    it('works', async () => {
      // const results = await GeoApiTransform.decompress(regionData, stateData)
      // expect(results)
      //   .to.be.an('object')
      //   .that.has.all.keys(
      //     'boundingCircle',
      //     'pal',
      //     'pal_routes',
      //     'restriction_section',
      //     'streamProperties',
      //     'stream_access_point',
      //     'tributary',
      //     'trout_stream_section'
      //   )
    })
  })
})
